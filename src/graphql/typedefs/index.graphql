# Custom Scalars
scalar JSON
scalar Upload

type Query {
  _empty: String
}

union ResponseData = User | Game

type Response {
  statusCode: Int!
  success: Boolean!
  message: String!
  data: ResponseData
}