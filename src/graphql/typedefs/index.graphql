# Custom Scalars
scalar JSON
scalar Upload

type Query {
  _empty: String
}

union ResponseData = User | LudoGameState | Tournament

type Response {
  statusCode: Int!
  success: Boolean!
  message: String!
  data: ResponseData
}